<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alimentos del Alba - Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="../stylesheets/historial-asistencia.css">
    <link rel="shortcut icon" href="../images/logo2.jpg" type="image/x-icon">
</head>
<body>
    <div class="container">
        <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
            <div class="col-md-3 mb-2 mb-md-0">
                <a href="/admin" class="d-inline-flex link-body-emphasis text-decoration-none">
                    Volver al Panel
                </a>
            </div>

            <ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
                <li class="nav-link px-2 link-secondary">Historial de Asistencia</li>
            </ul>

            <div class="col-md-3 text-end">
                <a href="/" class="btn btn-danger">Salir</a>
            </div>
        </header>

        <h1>Historial de Asistencia</h1>

        <% if (error) { %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>

        <% if (historial && historial.length > 0) { %>
            <div class="table-responsive"> <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>ID Asistencia</th>
                            <th>Nombre</th>
                            <th>Apellido</th>
                            <th>Cargo</th>
                            <th>Departamento</th>
                            <th>Cédula</th>
                            <th>Fecha</th>
                            <th>Hora Entrada</th>
                            <th>Hora Salida</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% historial.forEach(registro => { %>
                            <tr>
                                <td><%= registro.id_asistencia %></td>
                                <td><%= registro.nombre %></td>
                                <td><%= registro.apellido %></td>
                                <td><%= registro.cargo %></td>
                                <td><%= registro.departamento %></td>
                                <td><%= registro.cedula %></td>
                                <td><%= new Date(registro.fecha).toLocaleDateString() %></td>
                                <td><%= registro.hora_entrada %></td>
                                <td><%= registro.hora_salida || 'Pendiente' %></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div> <% } else { %>
            <p>No hay registros de asistencia disponibles.</p>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>